export class Tutor {
    constructor(id, name, school, major, acceptanceRate) {
      this.id = id;
      this.name = name; 
      this.school = school; 
      this.major = major;
      this.acceptanceRate = acceptanceRate;
    }
}